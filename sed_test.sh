#!/bin/bash 

echo "This is a test" | sed 's/test/big test/'

# This example uses the s command, s command substitues a 2nd text string for the first text string pattern, specified between
# the forward slashes. 
